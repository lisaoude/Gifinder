// selecting body

// const, because it's always the same element
// and there's only one of it

// selecting here so i only have to do it once
// are re-use it later

export const Body = () => {
  const body = document.querySelector('body')
  
  return body
}
