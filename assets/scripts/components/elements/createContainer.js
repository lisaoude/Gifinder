// creating div container

// let, because the div container can change
// in appearance and use and can be re-used later

export const Container = () => {
  const element = document.createElement('div')
  
  return element
}
