// creating footer container (empty)

// const, because I will use only one footer
// and the content stays the same everytime 

export const footerContainer = () => {
  const element = document.createElement('footer')
    
  return element
}
